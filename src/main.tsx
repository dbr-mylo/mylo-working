
import { createRoot } from 'react-dom/client'
import App from './App.tsx'
import './index.css'

// Simple entry point that renders the App component
createRoot(document.getElementById("root")!).render(<App />);
